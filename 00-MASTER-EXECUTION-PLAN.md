# AI Agent Ecosystem — Master Execution Plan

**Owner:** Mohit Pammu
**Purpose:** Build a modular, production-grade, multi-agent ecosystem that powers three (potentially four) data science portfolio projects, demonstrating industry-current agentic engineering practices for the job search.
**Cost constraint:** Zero additional spend. All tooling drawn from existing subscriptions (Claude Pro, ChatGPT Plus/Copilot) or free tiers (Ollama local, Gemini Flash free tier, GitHub Models). Full technology stack and rationale: `docs/architecture/tech-stack.md`.
**Working environment:** `ds-core` conda environment, Python 3.12, M5 Pro MacBook.
**Knowledge base:** 7 distilled reference cards derived from 10 Google/industry whitepapers on agentic systems (architecture, tools/MCP, context/memory, skills, evaluation, security, spec-driven production).

---

## How to Use This Document

This is the **single source of truth** for build order and progress. It does three jobs:

1. **Defines the build sequence** — what gets built, in what order, and why that order minimizes rework
2. **Tracks status** — every file/artifact has a status flag you update as we go
3. **Anchors session continuity** — see the companion document `01-SESSION-PROMPT-GUIDE.md` for exactly how to resume work in any session

**Status flags used throughout:**
- `NOT STARTED` — not yet begun
- `IN REVIEW` — drafted, currently being scrutinized/refined together
- `APPROVED` — finalized, locked, safe to build on top of
- `BUILT` — code/config implemented and tested against the approved spec

**Golden rule for this build:** No file moves to `BUILT` until its spec is `APPROVED`. No downstream file starts until its upstream dependency is `APPROVED` or `BUILT`. This is the discipline from the Spec-Driven Development framework (Day 5) — specs before code, one layer fully vetted before the next begins.

---

## GitHub Strategy

**Decision:** Public repos, split by concern (Option A). The ecosystem architecture is documented and pushed publicly alongside the three portfolio projects — not kept private. Rationale: the underlying concepts (MCP, SKILL.md, agent harnesses, evaluation pipelines) are drawn from public industry whitepapers, not proprietary IP. What's valuable to demonstrate is disciplined, spec-driven engineering practice and the ability to explain the architecture — not secrecy. Consistent commit activity across these repos is itself a signal to reviewers.

**Repo structure:**

| Repo | Contents | Depends on |
|---|---|---|
| `ai-agent-ecosystem` | `core/`, `skills/`, `mcp-servers/`, `docs/` — the shared framework | — |
| `healthcare-fraud-detection` | Project 1 implementation | `ai-agent-ecosystem` |
| `fantasy-football-intelligence` | Project 2 implementation | `ai-agent-ecosystem` |
| `financial-analysis-platform` | Project 3 implementation | `ai-agent-ecosystem` |

**Commit cadence:**
- Commit to `ai-agent-ecosystem` as each file in Phases 0–4 reaches `APPROVED` (spec) and again at `BUILT` (implementation) — i.e., two natural commit points per file
- Once a project enters Phase 5/6/7, commits split: framework-level changes still go to `ai-agent-ecosystem`, project-specific work goes to that project's own repo
- Each project repo's README links back to `ai-agent-ecosystem` and briefly explains the shared-framework relationship — this is the narrative that shows the compounding architecture story in an interview

**Commit message discipline (per Day 5 whitepaper's review guidance):** Use clear, descriptive messages tied to the master plan's file IDs, e.g. `0.10 AGENTS.md: initial approved draft` or `1.2 core/orchestrator: implement LangGraph state machine`. This makes the commit history itself a readable build log alongside the progress table in this document.

---

## Runtime Stack — Governing Structure for Cards 02–06

This dependency chain emerged from the Card 01 review process and should govern how every subsequent reference card and `core/` component is written, so that responsibilities stay cleanly separated with minimal overlap:

```
Agent Contract
      ↓
Execution Harness
      ↓
State Management
      ↓
Memory System
      ↓
Tool Layer
      ↓
Evaluation & Observability
      ↓
Security & Governance
```

**Binding constraints this implies for cards not yet written:**

- **Card 05 (Quality & Evaluation)** must keep **Observability** ("what happened" — traces, logs, runtime telemetry, cost tracking, tool execution history) and **Evaluation** ("was the result good" — golden dataset testing, LLM-as-judge scoring, KPI measurement, regression evaluation) as clearly separated sections, not conflated into one undifferentiated "quality" topic.
- **Card 05 or Card 06** must introduce a formal **failure taxonomy** — explicit classification of failure types (tool failures, model failures, data failures, validation failures, permission failures, timeout failures, human-approval failures). This is currently missing ecosystem-wide and directly strengthens the Validate/Decide stage of the execution loop (Card 01 §2) by giving "Decide" something concrete to branch on.
- Every card from 02 onward should be checked against this stack at approval time: does it respect the layer below it, and does it avoid re-defining a concern owned by another layer?

---

## Directory Structure (Target State)

```
ai-agent-ecosystem/
├── README.md
├── AGENTS.md
├── docs/
│   ├── reference/                     (7 distilled whitepaper cards)
│   ├── architecture/                  (model routing, tech stack, diagrams)
│   ├── specs/                         (BDD-style specs, one per feature)
│   └── governance/                    (review/QA artifacts — emerged organically, not originally planned)
│       ├── ecosystem-cohesion-review-rubric.md   (frozen v1.0)
│       ├── PHASE-0-CLOSURE-PLAN.md    (active, 10-stage sequential remediation plan — updated as stages complete)
│       ├── architecture-traceability-matrix.md   (Closure Plan Stage 2 — created, seeded from Stage 1 audit)
│       ├── adr/                       (Closure Plan Stage 8 — complete, ADR-001 through ADR-007)
│       ├── architecture-verification-specification.md   (Closure Plan Stage 9 — complete, 22 scenarios across 7 groups + performance §8)
│       └── cohesion-reviews/v1/       (versioned historical snapshot — claude-independent/, chatgpt-independent/, synthesis, reconciliation)
├── core/                              (orchestrator, harness, memory, eval, observability)
├── skills/                            (SKILL.md library — reusable across all projects)
├── mcp-servers/                       (tool connectors)
├── projects/
│   ├── 01-healthcare-fraud-detection/
│   ├── 02-fantasy-football-intelligence/
│   ├── 03-financial-analysis-platform/
│   └── README.md
├── tests/
└── .agents/
```

Full rationale for this structure was established in our architecture discussion — see `docs/architecture/tech-stack.md`.

---

## PHASE 0 — Foundation Documents

**Goal:** Establish the knowledge base and top-level specification before any code exists. Nothing in Phase 1+ starts until Phase 0 is fully `APPROVED`.

| # | File | Purpose | Status |
|---|---|---|---|
| 0.1 | `docs/reference/01-architecture-taxonomy.md` | Distilled: agent anatomy, 5-level taxonomy, 5-step loop | APPROVED |
| 0.2 | `docs/reference/02-tools-mcp.md` | Distilled: tool design, MCP architecture, A2A, NxM problem | APPROVED |
| 0.3 | `docs/reference/03-context-memory.md` | Distilled: session mgmt, 4 memory types, retrieval patterns | APPROVED |
| 0.4 | `docs/reference/04-skills-framework.md` | Distilled: SKILL.md anatomy, progressive disclosure, taxonomy. Must answer the procedural-memory-to-Skill promotion question raised in Card 03's review: when does procedural memory become a Skill, who approves it, can agents create/modify Skills, are Skills versioned, are Skills executable instructions or reference docs | APPROVED |
| 0.5 | `docs/reference/05-quality-evaluation.md` | Distilled: outside-in/inside-out eval, LLM-as-judge, flywheel | APPROVED |
| 0.6 | `docs/reference/06-security.md` | Distilled: 7-pillar security, zero ambient authority, sandboxing | APPROVED |
| 0.7 | `docs/reference/07-spec-driven-production.md` | Distilled: SDD, BDD/Gherkin, code review tiers, guardrails | APPROVED |
| 0.8 | `docs/architecture/tech-stack.md` | Final stack decisions: LangGraph, vector DB, model routing | APPROVED (frozen 1.0) |
| 0.9  | `docs/architecture/model-routing-table.md` | Which model/tier handles which task category (zero-cost design) | APPROVED (frozen 1.0) |
| 0.10 | `AGENTS.md` | Master ecosystem specification — the architectural North Star | APPROVED (frozen 1.0) |
| 0.11 | `README.md` | Top-level repo overview | APPROVED |
| 0.12 | `docs/architecture/agent-contract-template.md` | Standard specification template every agent is built against... | APPROVED |
| 0.13 | `docs/governance/ecosystem-cohesion-review-rubric.md` | 14-dimension rubric for evaluating Cards 01-07 as one integrated specification — not part of the original Phase 0 plan; emerged organically out of the Card 01-07 review cycles. Frozen at Version 1.0 | APPROVED (frozen v1.0) |
| 0.14 | `docs/governance/cohesion-reviews/v1/` | Per the rubric's own 6-phase process: two independent reviews → merged findings → consolidated action list → edits applied → final confirmation review. v1 cycle complete through Phase 3 (both independent reviews done, findings reconciled in `review-reconciliation.md`, action list consolidated in `PHASE-0-CLOSURE-PLAN.md`). Phases 4-6 (apply edits, final confirmation review) remain. Phase 0 is not considered fully closed until this completes with a Freeze-Ready verdict | IN REVIEW |
| 0.15 | `docs/governance/architecture-verification-specification.md` | 18 scenarios across 7 groups — planning artifact for Phase 1+ test suite; not executed until Phase 1 infrastructure exists | APPROVED |

**Exit criteria for Phase 0:** All 15 files `APPROVED` (12 originally planned + 2 that emerged organically from the review process: the cohesion rubric and the cohesion review cycle itself). AGENTS.md in particular must be scrutinized line by line since every later file inherits from it. The agent contract template (0.12) must be approved before any agent in Phase 1+ is specced. **Critically: Phase 0 is not closed simply because all 7 reference cards (0.1-0.7) are individually approved** — the ecosystem-cohesion review (0.14) must also reach a Freeze-Ready verdict per the rubric's own 6-phase process before Phase 1 begins. Individual card approval and ecosystem-level cohesion are two distinct gates.

---

## PHASE 1 — Core Infrastructure (The Factory)

**Goal:** Build the parts that never change across projects. This is the highest-leverage phase — flaws here propagate into all three projects. Housekeeping items carried over from Card 03's review cycle, to be addressed during this phase rather than pre-specified in the card: (1) per-type Memory Contracts (Card 03 §11) get drafted during 1.5/1.6, not before; (2) default context budget allocations (Card 03 §9) need concrete numbers (e.g., system/instructions reserved, history 20-30%, retrieved memory 20-30%, tool outputs bounded per tool, completion reserve always preserved) — defined at implementation time; (3) verification_status state-transition rules (who can mark verified/contested, can contested memory be injected) are Card 05/06 territory, consumed by Card 03's implementation once defined. Card 01 should also receive a brief terminology correction noting Card 03 renamed "structured state" to "Structured Records" to avoid colliding with Card 01's own `state` definition.

| # | File/Component | Purpose | Status |
|---|---|---|---|
| 1.1 | `docs/specs/orchestrator-spec.md` | BDD spec for the orchestrator agent | NOT STARTED |
| 1.2 | `core/orchestrator/` | Orchestrator implementation (LangGraph) | NOT STARTED |
| 1.3 | `docs/specs/harness-spec.md` | BDD spec for the agent harness (state, tools, feedback loops) | NOT STARTED |
| 1.4 | `core/harness/` | Harness implementation | NOT STARTED |
| 1.5 | `docs/specs/memory-spec.md` | BDD spec for memory system (working/episodic/semantic/procedural/preference/structured-records) | NOT STARTED |
| 1.6 | `core/memory/` | Memory implementation (local vector DB + PostgreSQL + structured stores, per Card 03) | NOT STARTED |
| 1.7 | `docs/specs/observability-spec.md` | BDD spec for OpenTelemetry logging (session/think/tool spans) | NOT STARTED |
| 1.8 | `core/observability/` | Observability implementation | NOT STARTED |
| 1.9 | `docs/specs/evaluation-harness-spec.md` | BDD spec for the 7-dimension evaluation pipeline | NOT STARTED |
| 1.10 | `core/evaluation/` | Evaluation harness implementation | NOT STARTED |

**Exit criteria for Phase 1:** A "hello world" agent can run through the full loop (mission → scan → think → act → observe), log a complete trace, store and retrieve a memory, and pass a basic evaluation check — with zero project-specific code involved.

---

## PHASE 2 — Security Layer

**Goal:** Wrap the Phase 1 factory in the production-grade safety net before any real data touches it. Card 06 (Security & Governance) is APPROVED and now fully specifies risk tiers, the Permission Model (ABAC+JIT), sandboxing, A2A trust boundaries, registry integrity, memory governance authorization, instruction-artifact signing, Policy Server integrity/failure modes, data classification with inheritance rules, HITL reviewer authorization, and break-glass governance — closing every forward-reference accumulated from Cards 01-05. Remaining items are genuine implementation detail (not architecture gaps), to be made concrete during this phase: full data classification taxonomy and per-class retention/deletion policy (Card 06 §13); named break-glass role list (Card 06 §20/§25); specific circuit-breaker trigger thresholds (Card 06 §11); JIT token storage mechanism (Card 06 §18); model-routing-table.md provider approval metadata (provider_approved_for_classes, retention_policy, zero_retention_available, etc. — Card 06 §26, tracked for Card 07/final cohesion); and the untrusted-context-boundary runtime mechanism (source labels, instruction/data delimiters, tool-output sanitization — Card 06 §26, a Harness/Context Engineering implementation item).

| # | File/Component | Purpose | Status |
|---|---|---|---|
| 2.1 | `docs/specs/sandboxing-spec.md` | BDD spec for ephemeral execution sandboxing | NOT STARTED |
| 2.2 | `core/security/sandbox/` | Sandbox implementation | NOT STARTED |
| 2.3 | `docs/specs/policy-server-spec.md` | BDD spec for the Policy Server (structural + semantic checks) | NOT STARTED |
| 2.4 | `core/security/policy_server/` | Policy Server implementation | NOT STARTED |
| 2.5 | `docs/specs/context-hygiene-spec.md` | BDD spec for PII masking / placeholder injection | NOT STARTED |
| 2.6 | `core/security/context_resolver.py` | Context resolver implementation | NOT STARTED |
| 2.7 | `docs/specs/circuit-breaker-spec.md` | BDD spec for intent drift detection + rollback | NOT STARTED |
| 2.8 | `core/security/circuit_breaker/` | Circuit breaker implementation | NOT STARTED |

**Exit criteria for Phase 2:** A deliberately malicious or malformed tool call is caught, logged, and gracefully refused by the system — verified with a test case.

---

## PHASE 3 — Skills Library

**Goal:** Build the reusable, swappable capability layer. Items deliberately deferred from Card 04 to be addressed during this phase: skill dependency declarations (`depends_on`/`optional_dependencies`/`conflicts_with` fields on the Skill Contract), skill freshness tracking (`last_reviewed`/`validation_date`), and — if a future ecosystem need arises — deeper composition precedence layers beyond the current Core→Domain split (Global→Core→Project→Task→Session).

| # | File/Component | Purpose | Status |
|---|---|---|---|
| 3.1 | `skills/data-ingestion/SKILL.md` | Generic data ingestion skill | NOT STARTED |
| 3.2 | `skills/eda/SKILL.md` | Exploratory data analysis skill | NOT STARTED |
| 3.3 | `skills/feature-engineering/SKILL.md` | Feature engineering skill | NOT STARTED |
| 3.4 | `skills/modeling/SKILL.md` | Model training/selection skill | NOT STARTED |
| 3.5 | `skills/evaluation/SKILL.md` | Model evaluation skill (distinct from agent evaluation in core/) | NOT STARTED |
| 3.6 | `skills/reporting/SKILL.md` | Stakeholder-ready reporting skill | NOT STARTED |

**Exit criteria for Phase 3:** Each skill loads correctly via progressive disclosure (metadata-only by default, full content on trigger) and has been smoke-tested with a trivial synthetic dataset.

---

## PHASE 4 — MCP Connectors

**Goal:** Build the tool-connection layer. Before or during this phase, two items deferred from Card 02 must be defined: (1) explicit transition criteria for each Tool Lifecycle stage (e.g., what specifically must be true for Designed→Security Reviewed), and (2) minimum tool-level certification/test requirements (schema validation, happy-path, failure-path, timeout, retry/idempotency, redaction/logging, data-minimization tests) referenced in Card 02 §6. These were deliberately left undefined in Card 02 to avoid over-specifying before any real tool existed.

| # | File/Component | Purpose | Status |
|---|---|---|---|
| 4.1 | `mcp-servers/file-system-connector/` | Local file system MCP server | NOT STARTED |
| 4.2 | `mcp-servers/postgres-connector/` | PostgreSQL MCP server (your existing PG18 setup) | NOT STARTED |
| 4.3 | `mcp-servers/api-connectors/` | Generic REST API MCP connector template | NOT STARTED |

**Exit criteria for Phase 4:** Orchestrator can discover and call each MCP server's tools successfully.

---

## PHASE 5 — Project 1: Healthcare Fraud Detection

| # | File/Component | Purpose | Status |
|---|---|---|---|
| 5.1 | `projects/01-healthcare-fraud-detection/README.md` | Project overview, scope, goals | NOT STARTED |
| 5.2 | `domain-skills/medicare-claims/SKILL.md` | Domain knowledge skill | NOT STARTED |
| 5.3 | Data MCP connector config (CMS Medicare) | Data source wiring | NOT STARTED |
| 5.4 | Unsupervised modeling pipeline | Isolation forest / clustering | NOT STARTED |
| 5.5 | Compliance-aware reporting agent | Output layer | NOT STARTED |
| 5.6 | Full evaluation run + documentation | Proof of working system | NOT STARTED |

*(Detailed file-by-file breakdown to be expanded once Phase 0-4 are approved — premature to over-specify this far ahead.)*

---

## PHASE 6 — Project 2: Fantasy Football Intelligence
*(To be expanded after Phase 5 begins — same reasoning as above.)*

## PHASE 7 — Project 3: Financial Analysis Platform
*(To be expanded after Phase 6 begins.)*

---

## Working Principles for Every File We Build

1. **Spec before code, always.** Every component gets a BDD-style spec (`Scenario / Given / When / Then`) reviewed and approved before implementation begins.
2. **One file at a time.** We do not parallelize drafting. Each file is read, scrutinized, revised, and approved before the next is opened.
3. **No silent assumptions.** If a design decision is ambiguous, it gets surfaced as a question, not resolved unilaterally.
4. **Token economy is a design constraint, not an afterthought.** Every spec considers which model tier (local Ollama / Gemini Flash / GitHub Models) it will run on, per the model routing table.
5. **Update this document after every session — explicitly including the status table row for every file touched, not just the Progress Log entry.** A Progress Log entry describing what changed is not sufficient on its own; the corresponding status table row (Phase 0's `#`/`File`/`Purpose`/`Status` table, or the equivalent table in later phases) must be checked and flipped in the same pass. This session's own experience is the cautionary example: three files (0.9, 0.10, 0.12) were correctly logged and matrix-updated at the time of completion, but their status table rows were never flipped from `NOT STARTED` — caught only during a later end-of-phase scan. The Progress Log and the status table are two different surfaces; updating one does not update the other.
6. **Build locally first, then push.** Every file is created and verified in the local `ds-core` environment before being committed. Commits follow the cadence and message convention defined in the GitHub Strategy section above.
7. **Every reference card carries its own version number, independent of the Master Plan's status flag.** Each card now opens with a `**Card Version:**` line (e.g., "1.0 (Approved)"). The Master Plan's `APPROVED`/`BUILT` status tracks *workflow state*; the card's own version line tracks *content state* and must be self-sufficient if the card is ever read in isolation, outside this repo. **Rule:** a card touched during the Phase 0 Closure Plan (or any future edit cycle) gets its version bumped (e.g., 1.0 → 2.0) at the point the edit is made, with a one-line changelog note added directly below the version line stating what changed and why. A card that is *not* touched during a given remediation cycle remains at its current version — version numbers are per-card and only increment on actual content change, never incremented in lockstep just because other cards changed.
8. **Run a full accuracy/consistency scan at the end of every phase.** Before a phase is marked closed, systematically check the Master Plan's status table, the Closure Plan's stage definitions (while still in Phase 0), the Traceability Matrix, and README against the actual state of every card/file — not just the files touched that session. This is what catches drift that accumulates silently across many small edits (a stale status flag, a citation typo, an undercounted stage) rather than relying on it being noticed by chance during routine work.

---

## Polish / Deferred Enhancements

Items that are genuinely worth doing but deliberately deferred to a better-timed moment in the build — tracked here so they aren't lost. **Grouped by the build phase that triggers them, soonest-phase-first; sorted soonest-within-phase.** When starting any new phase, check this list's corresponding section before writing the first line of that phase's code — this is the mandatory checkpoint that prevents rediscovering something mid-build that should have been designed in from the start.

**Status lifecycle:** every row starts `Pending`. When an item is implemented or decided, do **not** mark it `Done` and leave it here — check it against the Architecture Traceability Matrix and any cards it touches first (same "verify before marking complete" discipline as the Closure Plan stages, to avoid silently reintroducing an inconsistency), then add one Progress Log entry summarizing what was built/decided and why, then **delete the row from this list entirely**. This list should only ever contain unresolved future work; its permanent record lives in the Progress Log, identical to every other completed item in this project.

### Trigger: Still within Phase 0 (Closure Plan Stages 7-9)

| Item | Why deferred | Best time to revisit | Status |
|---|---|---|---|
| **Docs site** (e.g., `mkdocs` + GitHub Pages, turning `docs/` into a browsable site) | Premature with only 3-7 reference cards; pays off once specs, reference cards, and architecture docs are numerous enough that flat markdown browsing on GitHub becomes unwieldy | Once all 7 reference cards + Phase 0 architecture docs are approved | Pending |

### Trigger: Phase 0 → Phase 1 transition (after Closure Plan closes, before first Phase 1 code)

| Item | Why deferred | Best time to revisit | Status |
|---|---|---|---|
| **Dedicated research pass — scan remaining Claude Platform docs, LangGraph docs, and MCP spec for unincorporated mechanisms** | Mid-session scanning (Stage 7) surfaced real value early (Headroom, Token Counting, Effective Context Engineering, LangGraph checkpointers) but showed diminishing returns by the guardrails pass — confirmation outweighing new findings. Better done as one deliberate, complete pass at the natural transition point, where its findings directly inform the several build-vs-use-native decisions already queued in the Phase 1 table below, rather than continuing ad hoc mid-Closure-Plan and risking scope creep against the open stage | Once Closure Plan Stage 9 closes, before any Phase 1 code is written — remaining unscanned: Card 02's `agent-skills/best-practices`, `tool-combinations`, `parallel-tool-use`, `remote-mcp-servers`; the canonical MCP spec (modelcontextprotocol.io); Card 05's `test-and-evaluate` section generally; Card 06's broader `strengthen-guardrails` section beyond refusals/jailbreaks | Pending |

### Trigger: Phase 1 (Core Infrastructure)

| Item | Why deferred | Best time to revisit | Status |
|---|---|---|---|
| **Tool Search Tool** (Anthropic's `tool_search` mechanism — keeps tool definitions out of context until requested, instead of loading all schemas upfront) | Identified as the concrete discovery mechanism for Card 02 §5's Tool Registry pattern at scale — not needed until `core/tools/` registry-loading logic is being built, and tool count across 3 projects is still small enough not to require it yet. Anthropic's own guidance gives a concrete trigger: keep the 3-5 most-used tools always loaded, switch to dynamic discovery at 10+ — this is an instance of the general "just-in-time context via lightweight identifiers" pattern, not a standalone mechanism | Phase 1, when `core/tools/` implements Registry-backed tool loading — adopt once any agent's `tools_used` count approaches the 10+ threshold; evaluate against the plain Registry-lookup approach already specified below that threshold | Pending |
| **Context Editing — tool result clearing** (Anthropic's native `clear_tool_uses` mechanism, clears stale tool_result blocks with a placeholder) | A first-party alternative/complement to the Headroom-style compression layer below — needs a direct comparison before choosing one, since both solve the same Card 02 §3/§9 problem from different angles (native API feature vs. third-party library) | Phase 1, evaluated side-by-side with Headroom before `core/harness/` commits to one approach | Pending |
| **Token Counting API integration** (Anthropic's pre-flight `count_tokens` endpoint) for `core/harness/`'s budget enforcement | Identified as the concrete mechanism Card 03 §9 (Context Budget Management) and the Agent Contract's `token_budget` field (§8) need to actually be enforceable, rather than aspirational | Phase 1, when `core/harness/` implements context budget enforcement | Pending |
| **Headroom-style context compression layer** (or equivalent — compresses tool outputs/logs/RAG chunks before they reach the LLM, reversible) for `core/harness/` and tool-output handling | A concrete implementation candidate for Card 02 §3/§9's tool-output data-minimization discipline — evaluated, not yet adopted; any adoption must verify `source_type` tagging (Card 03 §12) survives compression, and must be compared against Context Editing above before choosing | Phase 1, alongside Token Counting and Context Editing evaluation | Pending |
| **Memory Tool** (Anthropic's first-party file-based memory directory, `/memories`, persisting across sessions) | A candidate implementation substrate for Card 03 §5's memory types — needs evaluation against the already-specified vector-store + PostgreSQL approach before `core/memory/` is built, not after. Also worth comparing against the simpler "structured note-taking" pattern (e.g., a single NOTES.md-style file) for cases where full memory-tool overhead isn't needed — note-taking suits iterative work with clear milestones, the Memory Tool suits genuine cross-session knowledge accumulation. **Distinct from LangGraph's own Checkpointer/Store split** (Checkpointer = short-term, thread-scoped, maps to Card 01's `state`; Store = long-term, cross-thread, maps to Card 03's Memory) — any memory-substrate decision must account for all three options (vector+Postgres, Anthropic's Memory Tool, LangGraph's native Store), not just the first two | Phase 1, when `core/memory/` is being designed — decide build-vs-use-native-vs-LangGraph-Store before any memory storage code is written | Pending |
| **Server-side Compaction** (Anthropic's auto-summarization when approaching context limits) | Validates an existing design choice (AGENTS.md as a durably-reloaded `system`-tagged file, per Card 03 §12) rather than introducing a new one — Anthropic's own docs warn compaction can silently summarize away rules that must always apply, which is exactly why AGENTS.md isn't relied on to survive via conversation history. Worth an explicit principle, not just a passive validation | Phase 1, when `core/harness/` designs its own long-running-session strategy — confirm this principle gets written into Card 03 §9 or `core/harness/`'s spec explicitly | Pending |
| **Subagent / fresh-context delegation as an explicit Card 01 §6 or Card 03 §9 principle** | Currently implicit in the multi-agent Coordinator pattern already specified — worth promoting to an explicit stated principle ("delegate large reads to a sub-agent specifically to protect the parent's context budget") rather than leaving it as an unstated convention. Anthropic's own guidance gives concrete economics worth citing directly: a sub-agent may use tens of thousands of tokens internally but should return only ~1,000-2,000 tokens to the parent — and a decision rule for which long-horizon technique fits which task shape (compaction → extensive back-and-forth; note-taking → iterative work with clear milestones; multi-agent → parallel research/analysis) | Phase 1-2, when the first Coordinator/sub-agent split is actually implemented — write the token-budget ratio and the technique-selection rule into the principle itself, not just the delegation pattern | Pending |
| **LangGraph reducer functions** as the enforcement mechanism for the Agent Contract's State Ownership field (§7) | LangGraph's checkpointer-based state can suffer real concurrent-mutation race conditions when multiple agents update shared state simultaneously — exactly the failure mode §7's `state_owned`/`state_read_only` fields were added to prevent. Reducer functions (explicit per-key merge logic) are the actual mechanism that turns that contractual declaration into an enforced constraint, rather than a documentation-only convention agents could still violate in code | Phase 1, when `core/harness/` implements its LangGraph state schema — reducers should be written per state key according to each agent's declared `state_owned` scope from its contract, not designed generically and reconciled with contracts after the fact |
| **`stop_reason: "refusal"` as a distinct Formal Failure Taxonomy category** (Card 05 §6) | Anthropic's API returns a formal `refusal` stop reason distinct from ordinary errors/timeouts — recovery requires resetting conversation context (removing/rephrasing the triggering turn, or clearing history) before continuing, which doesn't map cleanly onto any of Card 01 §2's existing Continue/Retry/Escalate/Stop outcomes as currently defined; a plain Retry would resubmit the same context and likely refuse again | Phase 1, when `core/evaluation/` implements the Formal Failure Taxonomy in code — needs a decision on whether "reset-and-retry" becomes a fifth Decide outcome or a specialized form of Retry |
| **Refusal-triggering content in untrusted tool output as a denial-of-service attack vector** (Card 06 threat model) | A poisoned file or tool output can contain a pattern that forces a hard model stop, silently halting an agent mid-task — structurally a new variant of the threat Card 03 §12/Card 06 §26 already address (untrusted content attempting to control agent behavior), except this variant forces a stop rather than seeking obedience as an instruction. Worth naming explicitly in Card 06's threat model as a DoS-style injection variant, distinct from the override-attempt variant already covered | Phase 1, when `core/security/` or `core/harness/` implements tool-output sanitization — decide whether this needs active scanning/handling or is sufficiently covered by treating all tool output as untrusted data already |
| **CI platform selection** (beyond the existing GitHub Actions badge in README) | Named in `tech-stack.md` as intentionally deferred — premature before Phase 1 implementation exists to actually run CI against | Phase 1, once `core/` has its first testable code | Pending |
| **Secret manager** (beyond current `.gitignore`/secret-scanning baseline) | Named in `tech-stack.md` as intentionally deferred — current baseline (gitignore patterns + GitHub secret scanning) is sufficient at zero-API-key-in-use stage | Phase 1-2, once any real API key/credential is actually needed at runtime | Pending |
| **Container registry** | Named in `tech-stack.md` as intentionally deferred — not needed until images are built and need to be stored/distributed beyond local Docker | Phase 1-2, once Docker images exist beyond local dev | Pending |
| **Cloud provider** | Named in `tech-stack.md` as intentionally deferred — Docker Compose (tech-stack.md Decision #8) keeps everything local for now | Phase 1-2/5+, tied to the same trigger as Decision #8's Kubernetes reconsideration (multi-host deployment need) | Pending |
| **Backup solution** | Named in `tech-stack.md` as intentionally deferred — no production data exists yet to back up | Phase 1-2, once PostgreSQL/pgvector holds real working data | Pending |
| **Scheduler** | Named in `tech-stack.md` as intentionally deferred — no recurring/scheduled job exists yet in the architecture | Phase 1-2, if/when a recurring job (e.g., the routing table's 30-day provider-evidence review) needs automation rather than manual execution | Pending |
| **Message queue** | Named in `tech-stack.md` as intentionally deferred — no asynchronous/decoupled workload exists yet; current architecture is synchronous per Card 01 §2's loop | Phase 1-2, only if a genuinely asynchronous multi-agent workload emerges that the synchronous loop can't serve well | Pending |
| **Runtime artifact-signature verification** (Card 06 §15/§16's full intent, beyond Git commit signing's provenance-only scope) | `tech-stack.md` Decision #11 clarified that Git commit signing satisfies provenance only, not runtime verification — a real, currently-open gap, not just a deferred nice-to-have | Phase 1, when `core/security/` implements §15/§16's signing enforcement — should not be pushed past Phase 1, since this is closer to a real gap than a future enhancement | Pending |
| **Visual architecture diagram** (SVG/PNG of the 7-layer Runtime Stack, for `docs/architecture/`) | The current text-based diagram is sufficient for active build work; a polished visual is most worth the effort once the stack is partially or fully built, so the diagram reflects reality rather than aspiration | Once Phase 1 (Core Infrastructure) is substantially complete | Pending |
| **Verification Specification schema enhancement** — add `Verification Type` (Functional/Security/Failure Mode/Recovery/Observability/Performance), `Priority` (Critical/High/Medium), and `Automation Target` (Unit/Integration/Simulation/End-to-End/Chaos) fields per scenario | Reviewed during Stage 9; all three fields are genuinely useful but premature at 18 scenarios — Verification Type and Priority become valuable for CI planning once scenarios are executable, Automation Target once the test suite actually exists. Adding them now is classification overhead without a consumer | Phase 1, once the first scenarios are being actively automated and CI planning requires scenario prioritization | Pending |
| **Playwright MCP** (`@playwright/mcp` — Microsoft's official browser automation MCP server) for `mcp-servers/` | Interactive browser automation via accessibility trees — handles JS-heavy sites, login sessions, and real-time dynamic pages that plain HTTP tools cannot. Direct use case in Fantasy Football Intelligence (ESPN/Sleeper/NFL.com) and Financial Analysis Platform (financial portals, filings); also the primary browser tool for the post-portfolio e-commerce agent. Needs a Tool Contract per Card 02 §4 before build, with an explicit sandboxing note: Playwright's browser process must be wrapped inside the Docker container boundary (Card 06 §5, tech-stack.md Decision #6), not just the agent calling it | Phase 1, when `mcp-servers/` receives its first tools — write the Tool Contract first, then register in the Tool Registry | Pending |
| **Firecrawl MCP** (`firecrawl-mcp` — official Firecrawl MCP server, keyless free tier for scrape/search/interact) for `mcp-servers/` | Web data extraction at scale — turns JS-heavy URLs into clean markdown or structured JSON, bypasses anti-bot, handles bulk extraction that Playwright MCP's session-based approach isn't optimized for. Direct use case in Fantasy Football (public stats/injury/news pages), Financial Analysis (earnings reports, SEC filings, financial news), and the post-portfolio e-commerce agent (product research, competitor monitoring). Tool Contract approval_basis: keyless tier, public data only — upgrade to paid tier only if a post-portfolio application runs at volume that justifies the cost | Phase 1, when `mcp-servers/` receives its first tools — write the Tool Contract first, then register in the Tool Registry; note that Playwright and Firecrawl are complementary (interactive sessions vs. bulk extraction) and both are likely needed | Pending |
| **gstack `/cso` skill** (Chief Security Officer — OWASP Top 10 + STRIDE threat model against code, 17 false-positive exclusions, 8/10+ confidence gate, independent finding verification with concrete exploit scenarios) | The single gstack skill that maps directly onto this architecture's existing concern: validates that Card 06's principles (ABAC+JIT enforcement, sandboxing, circuit breaker, audit trail, instruction-artifact signing) are actually implemented correctly in `core/security/` code, not just documented in the card. The 8/10+ confidence gate and false-positive exclusions make this genuinely signal-producing rather than noisy — each finding includes a concrete exploit scenario, which maps directly to Card 06's threat model. Install into your Claude Code environment, not into the ecosystem itself (`~/.claude/skills/gstack/cso`) | Phase 1, as soon as `core/security/` has its first implementation code — run `/cso` as the first external validation pass before any security-sensitive component goes further. Run again at the end of every phase that touches `core/security/` | Pending |

### Trigger: Phase 1-2, opportunistic (no hard dependency, adopt if convenient)

| Item | Why deferred | Best time to revisit | Status |
|---|---|---|---|
| **Obsidian as personal knowledge-output layer** (local vault for agent-generated outputs — trade logs, decision records, research summaries, e-commerce analytics — giving a navigable, graph-linked knowledge base for the trading agent and e-commerce agent) | No personal applications exist yet to write output to; adopting Obsidian as an output destination before the agents exist is premature — evaluate once the Post-portfolio applications actually begin generating data | Phase 1-2, once any Post-portfolio personal application (trading agent, e-commerce agent) begins producing structured outputs worth navigating; costs nothing since `docs/` can be opened as an Obsidian vault locally right now to evaluate the workflow before committing | Pending |
| **`kepano/obsidian-skills` Skills** (pre-built SKILL.md files for creating Obsidian-flavored markdown, Bases, JSON Canvas, and CLI interaction — directly loadable into `skills/`) | No agent currently needs to read/write an Obsidian vault; Skills only become relevant if the Obsidian-as-output-layer decision (above) is made. `obsidian-skills` follows the same SKILL.md specification as Card 04 — zero integration work, just copy the relevant Skills into `skills/` when needed. Obsidian Publish explicitly excluded — paid add-on ($20/month), revisit only after personal applications generate sustainable revenue | Phase 1-2, contingent on the Obsidian output-layer decision above being made first | Pending |
| **Programmatic Tool Calling** (Claude writes code that calls tools directly inside a sandbox; results never enter context) | Real efficiency gain for sequential/bulk tool chains, but explicitly not beneficial for single sequential calls per Anthropic's own benchmarks — relevant specifically to bulk-classification work, same shape as the Message Batches API question below | Phase 1-2, when any agent's tool-calling pattern is bulk/sequential rather than single-call — likely first relevant to Healthcare Fraud Detection's per-claim scoring | Pending |
| **`defer_loading: true` + prompt-cache interaction** for tool definitions | Niche cost optimization, only matters once tool count is large enough that cache-prefix stability is a real concern | Phase 2+, once tool count across all 3 projects grows past a handful | Pending |
| **Agent SDK's `bypassPermissions` mode — subagent inheritance, cannot be overridden** | If the Agent SDK's native permission system is ever used as the implementation substrate for Card 06 §4's ABAC+JIT downscoping, granting a parent agent `bypassPermissions` propagates that same unrestricted access to every subagent it spawns, with no per-subagent override — directly conflicting with §4's per-agent downscoping principle if not caught at design time | Phase 1-2, only if/when the Agent SDK's native permission system (rather than a custom Policy Server implementation) is chosen as `core/harness/`'s enforcement mechanism — irrelevant if Policy Server is built custom, as Card 06 §4 currently implies |
| **`nbstripout` pre-commit hook** for exploratory notebooks (strips output cells before commit, keeps source code) | Considered during a repo-security housekeeping pass — blanket-excluding `*.ipynb` in `.gitignore` was correctly rejected since it would exclude notebook source code, not just outputs; the right tool is a pre-commit hook, not a gitignore pattern | Phase 1-2, once any exploratory `.ipynb` work actually begins — not needed while no notebooks exist yet | Pending |
| **LLM Council multi-model cross-ranking pattern** for Card 05 §4's LLM-as-Judge evaluator tier | The anonymous peer-review-and-rank-then-synthesize pattern (fan out → anonymized cross-rank → Chairman synthesis) is a concrete implementation of the LLM-as-Judge tier that removes same-model favoritism bias. Not needed until Card 05's Evaluator Hierarchy is being implemented in `core/evaluation/`, and subject to the classification gate (Card 06 §26, model-routing-table.md) before routing evaluation calls to any non-local provider | Phase 1-2, when `core/evaluation/` implements the LLM-as-Judge tier — verify all evaluation inputs pass the classification gate before routing to non-local providers | Pending |
| **Graphify `/graphify docs/`** as a development query tool for the architecture | Parses any folder (code, docs, PDFs, markdown) into a queryable knowledge graph using Tree-sitter AST (zero tokens for code, local-only) — reduces context overhead for cross-cutting architecture questions during Phase 1 build, since `/graphify docs/` would map the entire card + governance set into a traversable graph. No architecture change, pure development tool | Phase 1, once `core/` starts filling in — run `/graphify docs/` to generate a queryable graph of the architecture before Phase 1's first major refactor | Pending |

### Trigger: Phase 3+ (multi-agent pipelines, bulk workloads live)

| Item | Why deferred | Best time to revisit | Status |
|---|---|---|---|
| **Critical vs. optional `runtime_dependencies` distinction** on the Agent Contract Template | Stage 7 production-readiness review (Revision 2) | Phase 3+, once degraded-mode logic for multi-agent pipelines becomes a live concern | Pending |
| **Message Batches API** for high-volume independent per-record classification steps (e.g., a per-claim fraud-likelihood scoring step in Healthcare Fraud Detection's pipeline) | Distinct from the interactive Plan→Act→Observe→Decide loop (Card 01 §2), which needs synchronous responses — batching applies specifically to bulk, independent, non-interactive classification work | Phase 3+, when Healthcare Fraud Detection's bulk-classification agent is actually being built — evaluate alongside Programmatic Tool Calling above, since both target the same bulk-workload shape | Pending |
| **`model_context_window_exceeded` as an explicit Formal Failure Taxonomy category** (Card 05 §6) | A real, citable failure mode surfaced by a platform docs scan — check whether Card 05 §6's existing categories already cover this generically before adding a new one | Phase 3+, or sooner if Card 05 §6 is revisited for any other reason — cheap to check, no urgency to force it | Pending |
| **Chaos verification scenarios** for the Architecture Verification Specification — Registry disappears mid-execution, database returns corrupted data, tool timeout during checkpoint write, Policy Server recovers mid-run, multiple simultaneous failures | Stage 9 external review correctly identified this as a future category — not Phase 1 because chaos engineering requires a stable baseline to chaos against | Phase 3+, once all Phase 1 scenarios pass consistently and the architecture is stable enough to deliberately break | Pending |
| **Upgrade verification scenarios** for the Architecture Verification Specification — policy update while runs are active, Registry reload during execution, model provider rotation, database migration | Stage 9 external review correctly identified this as a future category — not Phase 1 because upgrade scenarios require a running system with real state to upgrade | Phase 3+, once the architecture is running in production-equivalent conditions | Pending |

### Trigger: Phase 4+ (cross-version / fleet-scale concerns)

| Item | Why deferred | Best time to revisit | Status |
|---|---|---|---|
| **Contract Compatibility fields** (`minimum_contract_version`/`maximum_supported_version`) on the Agent Contract Template | Stage 7 production-readiness review | Phase 4+, if multi-agent cross-version compatibility becomes a live problem | Pending |

### Trigger: Phase 5+ (real data sources and models chosen)

| Item | Why deferred | Best time to revisit | Status |
|---|---|---|---|
| **Phase 5+ Compliance Package** — `provider-approval-registry.md` (split out of the routing table, referencing stable `provider_id`s rather than duplicating provider descriptions), `data-source-register.md`, `compliance-decision-log.md`, `deployment-security-baseline.md`, `provider-review-checklist.md`; plus approval-history tracking and event-driven (not just 30-day-polling) provider-terms-change detection for `core/harness/` | Two external architecture reviews of the routing table independently converged on the same boundary: this table should stay focused on runtime enforcement, not absorb every compliance-adjacent concern. Confirmed this session: all current/planned data sources are public or synthetic — no real PHI/PII/non-public financial records are used or planned. The package's real job once Phase 5 picks actual sources: confirm each source's **license/redistribution/public status** (a copyright question, separate from its privacy classification) — "public" and "license-clear for this specific use" aren't automatically the same thing | Phase 5+, when real data sources and specific provider/model/endpoint combinations are first chosen — do not pre-build this package against hypothetical sources | Pending |

### Trigger: Pre-publication (after all 7 cards frozen, before external-facing release)

| Item | Why deferred | Best time to revisit | Status |
|---|---|---|---|
| **Editorial pass on repeated cross-reference phrasing** (e.g., "mirroring Card 02" appears frequently across Cards 02-04) | Purely stylistic — repetition is fine and even helpful during active development for traceability, but reads better varied once frozen | A final editorial pass once all 7 cards are approved and frozen, before any external-facing publication of the reference docs | Pending |

**Review strategy note (from the Card 04 review cycle, applies going forward to Cards 05-07):** the foundation (Cards 01-04) is now strong enough that review emphasis should shift from "is content missing within this card" toward **cross-card consistency, governance symmetry, and hidden contradictions** between cards. This is the more likely failure mode in mature, multi-card architectures — keep this framing in mind for Cards 05, 06, and 07 specifically, since they sit furthest downstream and have the most surface area to silently contradict something already locked in.

### Trigger: Post-portfolio (architecture proven through all 3 reference projects)

| Item | Why deferred | Best time to revisit | Status |
|---|---|---|---|
| **Personal-use monetization applications** — candidates discussed: (1) a self-funded trading agent (small weekly budget, e.g. $20/week, human-approved trades only via existing Tier system/HITL gates, personal brokerage account, never advice to others — stays on the personal-tool side of the RIA regulatory line, not the advice-to-others side); (2) a digital-goods e-commerce agent (e.g. Etsy-style print/digital deliverables — agents handle research/design/listing/fulfillment, constrained by platform ToS on AI-disclosure and actual ownership/licensing of generated output) | Explicit, deliberate scope decision: this architecture is being built for **personal use and personal financial stakes only** — not commercialized, not sold, not marketed as advice or a product to others. This is a meaningfully different (and lower-risk, more achievable) goal than a platform business, and is the actual long-term vision once the 3 reference projects prove the architecture works. Revisiting now would be premature — these are real-world deployments requiring working infrastructure first | Post-portfolio, once Healthcare Fraud Detection, Fantasy Football Intelligence, and Financial Analysis Platform are built and have demonstrated the architecture works end-to-end | Pending |
| **Obsidian Publish** (hosted, browsable version of the Obsidian vault — could serve as a personal knowledge site or documentation layer for the trading/e-commerce agent's decision history) | Paid add-on ($20/month) — cost not justified until personal applications generate sustainable revenue; the local Obsidian vault (deferred above) provides the same value for free while that revenue is being built | Post-portfolio, once either the trading agent or e-commerce agent generates sustainable revenue sufficient to justify the add-on cost | Pending |
| **Perplexity MCP** (search-augmented AI queries via official Perplexity MCP server) | Requires a paid Perplexity API key — no meaningful free tier for API access, violating the zero-cost stack principle. Use case (real-time web research) is covered by Playwright MCP and Firecrawl MCP on the free/keyless tier for current project scope | Post-portfolio, if sustainable revenue from the trading agent or e-commerce agent justifies the API cost and a specific agent genuinely needs search-augmented reasoning that scraping cannot satisfy | Pending |

---

## Progress Log

*(Append entries here after each working session — newest at top.)* 

| Date | Session Focus | Files Touched | Outcome |
|---|---|---|---|
| 2026-07-11 | Applied Patch 4 (pre-freeze) — dependency flow terminology corrected | `docs/governance/adr/ADR-001-runtime-stack-ordering.md`, `AGENTS.md`, `00-MASTER-EXECUTION-PLAN.md` | Stage 10 reconciliation Patch 4: (1) ADR-001 Context section — "strict dependency hierarchy" replaced with "authority and ownership hierarchy"; Rationale section — added clarification paragraph explicitly distinguishing authority ordering from bidirectional runtime interaction flows (Security legitimately consumes observability signals from Evaluation, Circuit Breaker receives signals from Harness/Evaluation, Policy Server receives revocation requests from Circuit Breaker — all consistent with authority ordering, not in conflict with it). ADR-001 Changelog added with 2026-07-11 date. (2) AGENTS.md §4 — "layered stack" description updated to "authority and ownership hierarchy" framing, consistent with ADR-001's correction. AGENTS.md bumped 2.0 → 3.0 with stacked changelog. Card headers for Cards 02/03/05/06 confirmed not requiring changes — their governing-structure language already uses authority framing ("Security governs Tools, not the reverse") rather than the "strict dependency" phrasing that was the actual defect. Patch 4 acceptance criteria: all 5 checked. |
| 2026-07-11 | Corrected stale date formats across all affected files — Part B (file changelog headers, sign-offs, matrix, Stage 10 documents) | `docs/governance/adr/ADR-001` through `ADR-007` (5 Date fields), `AGENTS.md`, `docs/governance/architecture-verification-specification.md`, `docs/architecture/model-routing-table.md` (3 Approval date Sign-off fields), `docs/governance/architecture-traceability-matrix.md` (all Last Verified column dates), `docs/governance/cohesion-reviews/v1/stage-10-internal-rescore.md`, `docs/governance/cohesion-reviews/v1/stage-10-external-rescore.md`, `docs/governance/cohesion-reviews/v1/stage-10-reconciliation.md`, `00-MASTER-EXECUTION-PLAN.md` | Completed retroactive date standardization to YYYY-MM-DD format across all project files. Group 1: 5 ADR Date fields corrected to 2026-07-02 via targeted sed. Group 2: 3 Sign-off Approval date fields corrected (AGENTS.md and verification spec to 2026-07-10, model-routing-table to 2026-06-27). Group 3: Architecture Traceability Matrix Last Verified column corrected across all rows (Stages 1/3/4/5/6 → 2026-06-26, Stage 7 → 2026-06-28, Stage 7 follow-up → 2026-06-27, Stage 8 → 2026-07-02, Stage 9 → 2026-07-10). Group 4: Stage 10 internal rescore and reconciliation Date fields corrected to 2026-07-10; external rescore had no Date or Reviewer header fields at all — both added to match the internal rescore's format (Date: 2026-07-10, Reviewer: External). Date audit now complete across all files. |
| 2026-07-11 | Corrected 44 stale dates in Progress Log — retroactive date audit against git history | `00-MASTER-EXECUTION-PLAN.md` | Discovered every Progress Log entry and card/artifact changelog this session had used a placeholder-style "2026-06" date rather than tracking real calendar dates — some entries were genuinely June, but 14 commits spanning July 2-11 were also mislabeled "2026-06." Ran `git log --pretty=format:"%h %ad %s" --date=short` and manually cross-referenced all 44 Progress Log entries against their corresponding commit hash/date/message, one entry at a time with explicit confirmation at each step, rather than a bulk automated correction — chosen deliberately for full auditability given the sensitivity of retroactively editing a governance record. All 44 entries corrected to their accurate `YYYY-MM-DD` date (range: 2026-06-25 through 2026-07-11). Full audit table preserved in this session's history for future reference. Part B (correcting the same dates inside individual card/artifact changelog headers — Cards 01-07, AGENTS.md, tech-stack.md, agent-contract-template.md, model-routing-table.md, the verification spec, and all 7 ADRs) follows as a separate, subsequent commit. |
| 2026-07-11 | Applied Patch 2 (pre-freeze) — terminology defects resolved | `docs/reference/02-tools-mcp.md`, `AGENTS.md`, `docs/reference/04-skills-framework.md`, `docs/reference/06-security.md`, `docs/architecture/tech-stack.md`, `00-MASTER-EXECUTION-PLAN.md` | Stage 10 reconciliation Patch 2, all 5 edits: (1) Card 02 §3 heading corrected "Six" → "Seven Tool Design Principles" — section already enumerated seven, heading was wrong. Card 02 bumped 3.0 → 4.0. (2) AGENTS.md §4 "Layer 4 (Skills, Card 04) sits alongside Tools/Memory as a third capability type" corrected to "Skills (Card 04) occupy the same capability tier as Tools (Card 02) — not a separately numbered layer" — Skills were never actually a numbered stack layer, the original wording implied one. AGENTS.md bumped 1.0 → 2.0, its first content edit since freeze. (3) Card 04 §7 "executable procedural (or reference) knowledge" corrected to "runtime-loadable procedural (or reference) knowledge" — "executable" contradicted §1's own definition of a Skill as instructional content, not a callable function. Card 04 bumped 3.0 → 4.0. (4) Card 06 §4 — added a new "Terminology clarification — logical vs. implementation" paragraph distinguishing the logical Policy Authority role from its OPA WASM-embedded implementation (tech-stack.md Decision #7). Card 06 bumped 4.0 → 5.0. (5) tech-stack.md Decision #7 — added a clarifying sentence cross-referencing Card 06 §4's new terminology note. tech-stack.md bumped 1.0 → 2.0, its first edit since freeze. All 5 edits address Dimension 7 (Terminology Consistency) and Dimension 1 (Ownership Clarity) findings from the Stage 10 external review. Patch 2 acceptance criteria pending final verification after push/sync. |
| 2026-07-10 | Applied Patch 1 (pre-freeze) — artifact status discrepancies resolved | `AGENTS.md`, `docs/governance/architecture-verification-specification.md`, `00-MASTER-EXECUTION-PLAN.md` | Stage 10 reconciliation Patch 1: corrected AGENTS.md Status line from "Draft — awaiting review and approval" to "1.0 (Approved)" and verification spec Sign-off from "1.0 (Draft) / Approved by: pending" to "1.0 (Approved) / Mohit Pammu / 2026-06". Both artifacts were approved and frozen during Stage 7/Stage 9 respectively; their own status text failed to reflect that at the time. No version bumps — correcting a stale administrative field is not a content change. Patch 1 acceptance criteria: all four checked. |
| 2026-07-10 | Ran Closure Plan Stage 10 (Final Confirmation Review) — reviews complete, reconciliation produced | `docs/governance/cohesion-reviews/v1/stage-10-internal-rescore.md`, `docs/governance/cohesion-reviews/v1/stage-10-external-rescore.md`, `docs/governance/cohesion-reviews/v1/stage-10-reconciliation.md`, `00-MASTER-EXECUTION-PLAN.md` | Conducted dual-track Stage 10 re-score: internal re-score by architectural advisor (participated in all Closure Plan stages) and independent external cold-read re-score run concurrently. Both independently cleared the rubric's mandatory gate rule (no Freeze Criterion ≤4). Both independently verdicted Freeze-Ready-with-Minor-Patches. Reconciliation produced — dimensions where external and internal disagreed all resolved in favour of the external reviewer (the expected pattern when internal reviewer participated in building the artifacts). Reconciled scorecard: all 11 Freeze Criteria score 7-9 (lowest: Dimensions 2 and 9 at 7); all 3 Future Validation criteria score 7-8. Overall reconciled verdict: FREEZE-READY-WITH-MINOR-PATCHES. Five pre-freeze patches identified (artifact status discrepancies, terminology defects, lifecycle wording conflicts, dependency flow wording, verification spec overhaul) plus 5 mandatory Phase 1 pre-build specs. Reconciliation document enhanced with acceptance criteria per patch, patch verification matrix, out-of-scope section, future review triggers, and a formal Phase 0 Freeze Complete exit checklist. Five patches begin in the next session — Phase 0 is not declared closed until all 5 are applied and the exit checklist is fully checked. |
| 2026-07-10 | Research pass — LLM Council, gstack, Graphify, Playwright MCP, Firecrawl MCP, Perplexity MCP; Cowork and MCP infrastructure planning | `00-MASTER-EXECUTION-PLAN.md` | Evaluated 7 technologies/repos for fit against the existing architecture. Confirmed no card changes needed — all findings are either Tool Registry additions or development-workflow tools sitting above the architecture layer. Key findings: (1) Playwright MCP and Firecrawl MCP added as planned Phase 1 `mcp-servers/` build targets — together covering the full web-interaction surface (interactive sessions + bulk extraction) for Projects 2/3 and post-portfolio personal applications; each requires a Tool Contract per Card 02 §4 before build, with Playwright's sandbox constraint explicitly flagged (Card 06 §5). (2) Perplexity MCP deferred Post-portfolio — paid API violates zero-cost stack, use case covered by free alternatives. (3) LLM Council cross-ranking pattern deferred Phase 1-2 opportunistic — concrete implementation for Card 05 §4's LLM-as-Judge tier, subject to classification gate. (4) Graphify `/graphify docs/` deferred Phase 1 — development query tool for cross-cutting architecture questions, not an architecture component. (5) gstack, Obsidian, and Claude Cowork assessed separately earlier in this session. Affirmed that what is being built (a governed multi-agent runtime framework) sits at a different layer than gstack (slash-command workflow acceleration) — gstack would run on top of this ecosystem, not as part of it. Portfolio framing confirmed: the platform is the portfolio artifact; the three projects are proof-of-concept evidence it works. |
| 2026-07-10 | Ran Closure Plan Stage 9 (Architecture Verification Specification) — all housekeeping complete | `docs/governance/architecture-verification-specification.md`, `docs/governance/architecture-traceability-matrix.md`, `README.md`, `00-MASTER-EXECUTION-PLAN.md` | Finalized the Architecture Verification Specification after one external review pass — accepted 2 of 8 suggestions: Coverage Matrix (showing per-card scenario count and sections covered) and §8 (Architectural Performance Expectations, 4 scenarios). Added file 0.15 to the status table (verification spec was Stage 9's deliverable but had no status table row — same pattern as the rubric at 0.13 and cohesion review cycle at 0.14; corrected to keep tracking consistent). Updated Phase 0 exit criteria from 14 to 15 files. Deferred 3 items (Verification Type/Priority/Automation Target schema enhancements, Chaos scenarios, Upgrade scenarios). Directory Structure section in Master Plan and README governance tree both updated to reflect Stage 9 complete. Closure Plan stage count comment updated to "Stages 1-9 complete, Stage 10 next." Matrix's Architecture Verification Specification row updated from "Pending" to Stage 9 complete. Stage 9 exit criteria met: complete, citable verification plan with 22 scenarios exists, ready to execute the moment Phase 1 produces something to test against. Stage 10 is the last remaining Closure Plan stage. |
| 2026-07-10 | Ran Closure Plan Stage 9 (Architecture Verification Specification) | `docs/governance/architecture-verification-specification.md`, `docs/governance/architecture-traceability-matrix.md`, `00-MASTER-EXECUTION-PLAN.md` | Created the Architecture Verification Specification, discharging Synthesis B2. 18 scenarios across 7 groups (Policy Server, Circuit Breaker, Blast-Radius/Degradation, Execution Loop, Multi-Agent, Security Boundary, Observability) — each with Trigger/Expected/Pass Criterion/Execution Phase fields, all cited to the owning card section. Tagged as a planning artifact only — no scenario is executable until Phase 1 infrastructure exists. Put through one external review pass; accepted 2 of 8 suggestions: (1) Coverage Matrix added at the top of the document showing per-card scenario count and sections covered; (2) new §8 (Architectural Performance Expectations) added with 4 performance scenarios — architectural SLA expectations, not implementation benchmarks, baseline numbers deferred to Phase 1 instrumentation. Deferred 3 items to the Master Plan's Deferred list: Verification Type/Priority/Automation Target fields (schema enhancement, Phase 1 trigger), Chaos verification scenarios (Phase 3+), Upgrade verification scenarios (Phase 3+). Status table file 0.13 flipped to APPROVED. Stage 9 exit criteria met: a complete, citable verification plan exists, ready to execute the moment Phase 1 produces something to test against. |
| 2026-07-03 | README staleness fix — Stage 8 completion not reflected | `README.md` | Build Status section still described Stage 8 as upcoming; corrected to reflect Stage 8 complete. Added `adr/` directory to the governance tree in the Repository Structure section — directory was created during Stage 8 but not reflected in README. |
| 2026-07-03 | Investigated Obsidian + obsidian-skills — added 3 Deferred-list entries | `00-MASTER-EXECUTION-PLAN.md` | Scanned obsidian.md and kepano/obsidian-skills. Obsidian is a local-first, plain-text markdown knowledge base; `obsidian-skills` is a community-maintained set of SKILL.md files (same specification as Card 04) for agent-Obsidian interaction. Added 3 deferred entries: (1) Obsidian as a personal knowledge-output layer for the trading/e-commerce agents (Phase 1-2 opportunistic, costs nothing to evaluate since `docs/` opens as an Obsidian vault locally today); (2) `kepano/obsidian-skills` Skills as ready-made Card-04-compatible Skills to load when agents need vault interaction, contingent on (1) being adopted; (3) Obsidian Publish as a Post-portfolio consideration, explicitly deferred until personal applications generate sustainable revenue — $20/month not justified before that point. No card changes — this is all implementation-layer tooling that sits above the architecture. |
| 2026-07-02 | Resolved 2 Deferred-list items triggered by Stage 8 completion | `docs/architecture/agent-contract-template.md`, `00-MASTER-EXECUTION-PLAN.md` | Closed the two Stage 7-era deferred items whose triggers fired on Stage 8's completion. (1) `related_adr` field added to Agent Contract Template §16 — deferred since Stage 7 because ADRs didn't exist yet; now that ADR-001 through ADR-007 are in `docs/governance/adr/`, the pointer has real targets. (2) `implementation_reference` naming resolved — deferred since Stage 7 pending `tech-stack.md` settling language/packaging conventions; `tech-stack.md` (now frozen) deliberately avoids pinning directory/package conventions until Phase 1 code exists, so the field name stays `implementation_reference` with its description updated to make the "generic pointer, not hardcoded filesystem path" intent explicit. Both rows deleted from the Deferred list per the lifecycle rule — permanent record here in the Progress Log. Template bumped to Version 2.0 with stacked changelog. |
| 2026-07-02 | Ran Closure Plan Stage 8, Action 2 (7 ADRs) | `docs/governance/adr/ADR-001-runtime-stack-ordering.md` through `ADR-007-circuit-breaker-vs-policy-server-separation.md`, `docs/governance/architecture-traceability-matrix.md`, this plan | Created `docs/governance/adr/` and produced all 7 Architecture Decision Records, discharging Synthesis B3. Each ADR follows a consistent 7-section structure (Context → Alternatives Considered → Decision → Rationale → Tradeoffs → Consequences → Decision Status/Review Trigger → References) developed iteratively through individual reviews of ADRs 001-005 before being applied uniformly to all 7. Every ADR cites its canonical card sections without restating card content, cross-references related ADRs to form a coherent architectural narrative (ADR-001→002→003→004→005 as a chain; ADR-006/007 formally promote Stage 4's "Alternative Architecture Considered" in-card notes to governance-level records). Key decisions captured: Runtime Stack ordering and Security-at-base rationale (ADR-001); Policy Server centralization and why distributed authorization fails (ADR-002); the Contract Pattern shared across all four governed artifact types (ADR-003); Skills as a distinct promoted-procedural-memory artifact type rather than code or raw memory (ADR-004); Evaluation detects/Security enforces — the judgment-from-authority separation (ADR-005); Tool Registry and Skill Registry kept separate due to different governance semantics (ADR-006); Circuit Breaker and Policy Server kept separate due to Decide/Act separation (ADR-007). Stage 8 exit criteria met: Card 06 is navigable without a full read-through; every major architectural decision this review process surfaced has a permanent, citable record independent of any chat history. |
| 2026-07-02 | Ran Closure Plan Stage 8, Action 1 (Card 06 topic index) | `docs/reference/06-security.md` | Added Topic Index section after Card 06's Purpose line, mapping all 26+ sections into topic clusters (Risk Tiers, Policy Server, Circuit Breaker, Registries, Classification, A2A, JIT, HITL, Break-glass, Audit Trail, etc.) for fast lookup without requiring a full read-through — discharging Synthesis B1. Card 06 bumped 3.0 → 4.0 with stacked changelog. Stage 8, Action 2 (7 ADRs) remains. |
| 2026-06-28 | README staleness fix — Stage 7 completion not reflected | `README.md` | Sync-confirmation check after the prior staleness fixes surfaced a third gap: README's Repository Structure tree and Build Status section both still described Stage 7 as "in progress" with `model-routing-table.md`/`tech-stack.md` "remaining" — false since all four Stage 7 artifacts are now complete. Corrected the architecture/ tree comment, the governance/ tree's Closure Plan stage count comment (now "Stages 1-7 complete, Stage 8 next"), and the Build Status paragraph. This is the third staleness catch in this single pre-push scan (status table 0.8, the Directory Structure forward-reference, now README) — reinforces that a single fix often doesn't surface every place a fact is duplicated; the matrix, status table, and README all needed independent checking. |
| 2026-06-28 | Closed out Closure Plan Stage 7 entirely — all 4 artifacts complete; pre-push staleness scan caught 2 issues | `00-MASTER-EXECUTION-PLAN.md`, `docs/governance/architecture-traceability-matrix.md` | `tech-stack.md`'s completion makes this the fourth and final Stage 7 artifact (Agent Contract Template, AGENTS.md, model-routing-table.md, tech-stack.md all now frozen at 1.0). Ran a full staleness scan before logging, per Working Principle #8, rather than assuming only the matrix needed updating. Found: (1) status table's file 0.8 still read `NOT STARTED` — same recurring bug class as 0.9/0.10/0.12 earlier this session — corrected to `APPROVED (frozen 1.0)`; (2) the Directory Structure section's forward-reference to `tech-stack.md` still said "once written" — now false — corrected to a plain citation. Checked `PHASE-0-CLOSURE-PLAN.md` directly for any stage-completion staleness; found none, since that file describes the plan rather than tracking completion status itself (the Master Plan's status table is the actual tracker). Matrix's tech-stack.md row updated to reflect completion, with an explicit note that all four Stage 7 artifacts are now done. Stage 7 exit criteria met: all four files exist with real content, each internally consistent with the corrected Cards 01-07. Ready to move to Stage 8 (Card 06 topic index, 7 ADRs). |
| 2026-06-28 | Corrected README/Master Plan duplication — LangSmith/OpenTelemetry inconsistency resolved | `README.md`, `00-MASTER-EXECUTION-PLAN.md` | Now that `tech-stack.md` is frozen as the canonical technology-decision record, corrected the two places that previously restated (and disagreed on) observability tooling: README's Tech Stack section ("OpenTelemetry-style" generic) and the Master Plan's header ("LangSmith free tier") gave two different answers to the same question. Both corrected to point to `docs/architecture/tech-stack.md` rather than restating specifics — README's observability line now names OpenTelemetry + self-hosted Jaeger matching the actual decision; the Master Plan's cost-constraint line drops the stale LangSmith mention and adds an explicit pointer. This is the same pointer-not-restatement pattern applied to Card 01 §8 (Stage 3) and Card 07 §11 (post-Stage-7) — applied here to non-card documents for the first time. |
| 2026-06-28 | Discussed long-term vision — personal-use monetization, not platform commercialization | `00-MASTER-EXECUTION-PLAN.md` | A platform-business document was reviewed and discussed (Linux/Kubernetes-style commercialization, open-source-core-plus-paid-services, vertical SaaS, marketplace). Concluded this is explicitly not the goal — the actual vision is building this architecture for personal use, tested through the 3 reference projects, then applied to small-scale personal applications funded and run by and for the owner alone (e.g., a self-funded trading agent with human-approved trades, a personal digital-goods e-commerce operation) — never sold, marketed, or commercialized as a product/platform to third parties. This avoids the regulatory/licensing complexity that would come with selling the architecture or managing others' money, while still giving the architecture real-world stakes to prove itself against beyond synthetic portfolio data. Logged as a new "Post-portfolio" Deferred-list phase group, distinct from the existing Phase 3-5 build-sequence groups since it's a horizon beyond the build sequence itself, not a stage within it. |
| 2026-06-28 | Repo security housekeeping — .gitignore hardening, secret scanning confirmed, branch protection deferred | `.gitignore` (root), `00-MASTER-EXECUTION-PLAN.md` | Reviewed GitHub branch protection rules and rulesets for repo access control. Conclusion: with zero added collaborators, no one but the owner can currently write to this repo regardless of settings — branch protection/PR-gating mostly defends against multi-collaborator merge risk that doesn't exist yet, and would add real friction to the current fast solo-iteration pace for no real benefit. Deferred branch protection/rulesets to Phase 1+ (when collaborators or real implementation code with bugs that matter exist). Identified the two risks that actually apply now: accidental force-push/branch-deletion (history loss) and accidental secret commits. Added secret/credential file patterns (`*.key`, `*.pem`, `*.crt`, `secrets/`, `credentials.json`) to `.gitignore`, which previously only excluded `.env`. Confirmed via screenshot that GitHub's Secret Protection and Push Protection (under Advanced Security) are already enabled by default for this public repo — no manual enabling needed, correcting an initial assumption that they required opt-in. Considered and correctly rejected a blanket `*.ipynb` gitignore exclusion (would have excluded notebook source code, not just outputs) — added `nbstripout` as a Phase 1-2 deferred item instead, the correct tool for that specific goal. |
| 2026-06-27 | Corrected third related matrix row — Classification-Aware Model Routing | `docs/governance/architecture-traceability-matrix.md` | Continuing the same sync-confirmation pass that caught the two prior stale rows: this row described Card 07 §11 as "the schema that operationalizes" classification-aware routing — accurate before the §11 pointer fix, imprecise after it, since §11 no longer contains the schema. Corrected to explicitly state §11 is a pointer, with the schema itself cross-referenced to `model-routing-table.md` §4. This closes out all three matrix rows touched by the Card 07 §11 correction — no further related rows found. |
| 2026-06-27 | Corrected second stale matrix row — Provider Approval Metadata Schema | `docs/governance/architecture-traceability-matrix.md` | Sync confirmation after the Card 07 §11 pointer fix surfaced a second, separate matrix row (dating to Stage 1) still naming Card 07 §11 as the canonical owner of the provider-metadata schema — missed when the `model-routing-table.md` artifact row was updated, since these are two distinct rows describing the same underlying concept. Corrected the canonical owner to `model-routing-table.md` §4, consistent with the artifact row and Card 07 §11's actual current content. No further stale references to this schema's ownership found. |
| 2026-06-27 | Edited Card 07 §11 — pointer correction, post-Stage-7 follow-up | `docs/reference/07-spec-driven-production.md`, `docs/governance/architecture-traceability-matrix.md`, this plan | Card 07 §11 previously restated a 6-field provider-metadata schema that the frozen `model-routing-table.md` (Stage 7, Action 3) substantially exceeded (21 fields) — the exact duplication pattern that caused Card 01 §8's staleness, which Stage 3 corrected by replacing a restated list with a pointer. Applied the identical fix here: §11 now points to `model-routing-table.md` §4 as the canonical schema owner instead of maintaining its own copy. Card 07 bumped 1.0 → 2.0 — its first edit since the original freeze. Matrix's model-routing-table.md row updated to reflect Card 07 §11 as a corrected pointer, not an open flag. This closes the only outstanding follow-up from Stage 7, Action 3. |
| 2026-06-27 | Corrected status table — files 0.9, 0.10, 0.12; strengthened Working Principle #5 | `00-MASTER-EXECUTION-PLAN.md` | Caught during sync confirmation: three files (model-routing-table.md, AGENTS.md, agent-contract-template.md) were correctly logged in the Progress Log and the matrix at the time each was completed, but their status table rows were never flipped from `NOT STARTED` to `APPROVED`. Corrected all three. Strengthened Working Principle #5 to explicitly name the status table as a separate surface requiring its own check, not assumed to update alongside the Progress Log — this is the second occurrence of this exact gap (0.11/README earlier this session was the first), so the principle itself needed sharpening, not just another one-off fix. |
| 2026-06-27 | Ran Closure Plan Stage 7, Action 3 (model-routing-table.md) | `docs/architecture/model-routing-table.md`, `docs/governance/architecture-traceability-matrix.md`, this plan | Drafted and froze the model routing table at version 1.0, operationalizing Card 01 §4's specialist-model routing principle and Card 06 §13/§23/§26's classification-aware routing requirement. Put through two external production-architecture review passes; both independently converged on the same verdict — architecturally compliant, not legally certified compliant, and that distinction matters (the file is a policy enforcement specification, not a compliance document). Adopted schema substantially exceeds Card 07 §11's originally-specified minimum (product surface, model status, account type, evidence/expiration metadata, feature flags) — flagged for a follow-up Card 07 edit to bring §11 in line with what was actually built. Confirmed during this session: all current/planned project data sources are public or synthetic, no real PHI/PII/non-public financial records used or planned — the conservative fail-closed-to-Ollama default is a defensive engineering posture, not evidence of an existing regulatory obligation. Wording corrected twice per review feedback: "legal/security review" → "documented organizational/compliance approval" throughout (the architecture doesn't perform legal review, humans do), and "potential/future approval path only" → deterministic "not approved until evidence satisfies this table's requirements." Removed an earlier draft's CMS-SynPUF-specific note in favor of a source-agnostic open question, since naming a specific Project 1 data source is a Phase 5 scoping decision, not a Phase 0 infrastructure decision. Added Section 11, citing NIST AI RMF 1.0 and the CSA-published NIST AI RMF Agentic Profile as external reference frameworks the table's structure is independently consistent with — reference only, not adopted as binding authority. Both reviews recommended a future Phase 5+ compliance package (separate Data Source Register, Provider Approval Registry, Deployment Security Baseline, Compliance Decision Log) — correctly deferred, not built now, since it would be designed against hypothetical data sources/models that don't exist yet. Three of four Stage 7 artifacts now complete — `tech-stack.md` remains. |
| 2026-06-27 | Added Working Principle #8 — end-of-phase accuracy/consistency scan | `00-MASTER-EXECUTION-PLAN.md` | Triggered by this session's systematic scan catching 2 pre-existing defects (README's Closure Plan stage undercount, the matrix's Card 06 §39 citation error). Codified the practice as a standing Working Principle rather than a one-time deferred task — distinct from the Polish/Deferred list's "Phase 0 → Phase 1 transition research pass" entry, since this is a recurring discipline triggered at every phase boundary going forward, not a single scheduled event. Kept deliberately simple: end-of-phase cadence only, no denser sub-schedule during Phase 0's Closure Plan stages — relying on the existing per-file logging discipline to catch drift in between. |
| 2026-06-27 | Systematic accuracy/consistency scan — caught 2 pre-existing defects | `README.md`, `docs/governance/architecture-traceability-matrix.md`, this plan | Triggered by catching file 0.11's stale status flag — ran a full scan of the Master Plan status table, the Closure Plan's own stage definitions, and the matrix against the live cards. Found: (1) README's Build Status described the Closure Plan as "9-stage," undercounting by one — the Closure Plan's own Summary Table defines Stage 0 (procedural) through Stage 10, with Stage 10 (re-scoring the rubric against the corrected cards) being the actual closure gate, previously missing from README's description entirely. Corrected to "10-stage," explicitly naming Stage 10. (2) The matrix's Risk Tiers row cited "Card 06 §39" for the risk-tier table — Card 06 has no §39; the table actually lives at §3, where it's always been correctly cited everywhere else in this project. Corrected. Both appear to be pre-existing inaccuracies (the matrix error likely dates to its original Stage 2 drafting), not introduced this session. No card content changed — Master Plan and matrix bookkeeping only. |
| 2026-06-27 | Corrected status table — file 0.11 (README.md) | `00-MASTER-EXECUTION-PLAN.md` | File 0.11's status was still `NOT STARTED`, left stale from before this session's README refresh (Repository Structure tree updated, Build Status section rewritten with real Closure Plan progress). Corrected to `APPROVED`, reflecting that the file was substantively rewritten and pushed earlier this session. Caught during a routine sync-confirmation pass — exactly the kind of staleness this project's whole discipline exists to prevent, just inside the Master Plan's own status table rather than a reference card. |
| 2026-06-27 | Added "Phase 0 → Phase 1 transition" research-pass entry to Polish/Deferred Enhancements | `00-MASTER-EXECUTION-PLAN.md` | Mid-Stage-7 platform-docs scanning (Headroom, Token Counting, Effective Context Engineering, LangGraph checkpointers, guardrails/refusal handling) produced real findings but showed diminishing returns by the final pass — judged scope creep against the still-open Closure Plan stage rather than a Stage-7 blocker. Added a new phase-trigger group ("Phase 0 → Phase 1 transition") to the Deferred list, scheduling one deliberate, complete research pass at the natural transition point instead of continuing ad hoc — explicitly listing what remains unscanned (Card 02's Skills/tool-combination/parallel-tool-use/remote-MCP docs, the canonical MCP spec, Card 05's test-and-evaluate section, Card 06's broader guardrails section) so nothing already-scanned gets redundantly redone and nothing queued gets lost. Returning to Closure Plan Stage 7's remaining two artifacts (`model-routing-table.md`, `tech-stack.md`) now. |
| 2026-06-27 | Edited Card 03 §12 — Anthropic API tool_result routing note | `docs/reference/03-context-memory.md` | Added "Implementation Note (Anthropic API)" to §12's existing non-override rule, specifying the concrete model-call-level mechanism: non-`system` source_type content must route through `tool_result` blocks specifically, not just be tagged — Claude is trained to apply more skepticism to instructions inside `tool_result` content than content delivered as direct system/user text. Sourced from a Claude Platform docs scan (Phase 0, post-Closure Plan, platform/framework research pass — guardrails/jailbreak-mitigation section). Card 03 bumped 4.0 → 5.0 with stacked changelog. |
| 2026-06-27 | Edited Card 01 §2 — LangGraph checkpointer durability note | `docs/reference/01-architecture-taxonomy.md` | Added "Implementation Note (LangGraph)" to §2's existing crash-recovery blast-radius statement, clarifying that the guarantee depends on a durable checkpointer backend (e.g. `PostgresSaver`, `SqliteSaver`) — LangGraph's default `MemorySaver` only persists in-process RAM and is lost on restart, which would make the blast-radius statement false in practice if silently defaulted to in production. Sourced from a LangGraph docs scan (Phase 0, post-Closure Plan, platform/framework research pass). Card 01 bumped 3.0 → 4.0 with stacked changelog. |
| 2026-06-27 | Added 3 items to Polish/Deferred Enhancements from Claude Platform guardrails/security docs scan | `00-MASTER-EXECUTION-PLAN.md` | Added: (1) `stop_reason: "refusal"` as a candidate fifth Formal Failure Taxonomy category (Card 05 §6) — refusal requires context-reset recovery, not a plain Retry, and doesn't cleanly map onto Card 01 §2's existing four Decide outcomes; (2) refusal-triggering content in untrusted tool output as a denial-of-service injection variant for Card 06's threat model — distinct from the override-attempt variant Card 03 §12/Card 06 §26 already cover; (3) Agent SDK's `bypassPermissions` mode and its subagent-inheritance trap (cannot be overridden per-subagent), flagged only as relevant if the Agent SDK's native permission system — rather than a custom Policy Server — is ever chosen as Card 06 §4's enforcement substrate. All three added to the Phase 1 / Phase 1-2 sections per the existing phase-grouped structure, Status `Pending`. |
| 2026-06-27 | Reorganized Polish/Deferred Enhancements + scanned Claude Platform docs for unincorporated mechanisms | `00-MASTER-EXECUTION-PLAN.md` | Restructured the Deferred list from a single flat soonest-trigger-first table into 6 phase-grouped sections (Phase 0 remainder, Phase 1, Phase 1-2 opportunistic, Phase 3+, Phase 4+, Pre-publication) to keep it navigable as it grows — each phase becomes a mandatory checkpoint to consult before that phase's first line of code, rather than relying on memory to surface a buried row. Added a `Status` column (`Pending`/`Done`) with an explicit lifecycle rule: items are checked against the Traceability Matrix before being marked resolved, logged once here in the Progress Log, then deleted from the Deferred list entirely — the list holds only unresolved future work, never a growing graveyard of completed rows. Scanned Claude Platform docs (context windows, compaction, context editing, tool-context management, memory tool, programmatic tool calling) for mechanisms not yet incorporated into the architecture. Added 6 new items: Tool Search Tool (Card 02 §5's Registry-discovery mechanism at scale), Context Editing's native tool-result clearing (explicitly flagged for side-by-side comparison against Headroom before `core/harness/` commits to either), Memory Tool (a candidate implementation substrate for Card 03 §5, flagged for evaluation against the already-specified vector-store + PostgreSQL approach before `core/memory/` is built), Server-side Compaction (validates the existing AGENTS.md-as-durably-reloaded-system-context design choice rather than introducing a new one — flagged to become an explicit stated principle in Card 03 §9 or `core/harness/`'s spec, not just a passive validation), Programmatic Tool Calling (cross-referenced against the Message Batches API entry, since both target the same bulk-workload shape), and `model_context_window_exceeded` as a possible explicit Formal Failure Taxonomy entry (Card 05 §6) pending a check of whether it's already covered generically. Visual architecture diagram item's phase trigger corrected from unphased to explicitly Phase 1. No card content changed — this is Master Plan bookkeeping only. |
| 2026-06-27 | Ran Closure Plan Stage 7, Action 2 (AGENTS.md) | `AGENTS.md`, `docs/governance/architecture-traceability-matrix.md`, this plan | Drafted the master ecosystem specification at repo root as a signed instruction artifact per Card 06 §15, citing the corrected Tier table (§3/§14) and operationalized context-boundary mechanism (Card 03 §12) directly rather than restating either. Put through one external production-architecture review; accepted 4 of 5 recommendations — shortened Purpose, compressed "What This Ecosystem Is," replaced the Project-Specific table's implementation detail with pure references (Card 06 §14 + Agent Contract only), removed historical Closure Plan references from the "What This File Does Not Do" section to keep it timeless — plus accepted a new "Repository Philosophy" section, reworded from the reviewer's draft to cite the Traceability Matrix's single-owner discipline rather than implying the Runtime Stack itself adjudicates artifact disagreements. Rejected one recommendation: kept the "Security governs those layers, never the reverse" sentence in the Runtime Stack section, judged the highest-value-per-token sentence in that section rather than redundant restatement. Filled Section 12 (now reference-only) with real project data pulled from `README.md` (Healthcare Fraud Detection, Fantasy Football Intelligence, Financial Analysis Platform) rather than leaving it a placeholder. File confirmed to belong at repo root, not `docs/architecture/`, consistent with the standard AGENTS.md convention — distinguishing it from the other three Stage 7 artifacts, which do live in `docs/architecture/`. Two of four Stage 7 artifacts now complete — model-routing-table.md and tech-stack.md remain. |
| 2026-06-26 | Ran Closure Plan Stage 7, Action 1 (Agent Contract Template) | `docs/architecture/agent-contract-template.md`, `docs/governance/architecture-traceability-matrix.md`, this plan | Drafted the full field-by-field Agent Contract Template per Card 01 §1's specification, cross-referencing the now-fully-corrected card set (Stages 3-6) throughout rather than restating any owned concept. Put through two external production-architecture review passes; accepted 7 substantive additions across both passes — Dependencies, State Ownership, Execution Constraints, Failure Recovery Strategy (scoped to map onto Card 01 §2's existing four Decide outcomes, explicitly closing the loop against future invented outcomes), Assumptions, Artifact References, and a sharpened Blast Radius framing ("what capability is unavailable" vs. "what happens to this agent") — plus formatting tightening on Mission/Success Criteria/Stop Conditions toward single-sentence/checklist/bullet conventions. Renamed `owner` to `contract_owner` for disambiguation. Four items explicitly deferred rather than added or silently dropped (Contract Compatibility fields, critical/optional dependency distinction, `related_adr` field, `implementation_reference` naming) — added to the existing Polish / Deferred Enhancements section, which was also re-sorted soonest-trigger-first (was chronological-by-review-session) and had its bolding made consistent across all entries. Rejected outright, no tracking needed: Risk Matrix, Permission Tables, Tool/Memory Schemas, Evaluation Rubrics, Security Controls, display_name, and a standalone Change History table (the existing stacked-changelog convention already covers this). Matrix's combined "Four Missing Foundational Artifacts" placeholder row split into four individual rows now that one is complete. This is the first of the four Stage 7 artifacts — Stage 7 itself remains open pending AGENTS.md, model-routing-table.md, and tech-stack.md. |
| 2026-06-26 | Ran Closure Plan Stage 6 (Close the Blast-Radius Gaps) | `docs/reference/03-context-memory.md`, `docs/reference/02-tools-mcp.md`, `docs/reference/04-skills-framework.md`, `docs/reference/05-quality-evaluation.md`, `docs/reference/01-architecture-taxonomy.md`, `docs/governance/architecture-traceability-matrix.md`, this plan | Extended Card 06 §20's proven fail-closed-by-tier pattern to the four components the rubric's Dimension 13 flagged as lacking explicit blast-radius statements, each stating both halves per Dimension 13's requirement (what breaks AND what still works). Card 03 §12: Memory backend unavailability degrades reads (in-session state + non-memory context only, explicitly flagged in trace) rather than hard-failing, with Tier 2+ Structured Records reads deferring to Card 06 §20 instead. Card 02 §5 / Card 04 §6: whole-registry-file unavailability (distinct from Card 06 §16's single-bad-entry case) fails closed system-wide for new bindings, while in-flight JIT-tokened calls may still complete — documented symmetrically on both the Tool and Skill Registry sides. Card 05 §5: Evaluation-service unavailability never blocks the Harness (Observability has no dependency on Evaluation), backfilling once recovered, except Tier 4-5 HITL escalations gated on a Safety flag, which fall back to Card 06 §20 regardless. Card 01 §2: involuntary Harness/Orchestrator crash (distinct from a deliberate `Stop`) recovers from last persisted `state` checkpoint, treating unconfirmed in-flight calls as failed under Card 02's existing idempotency discipline rather than assuming success. Five cards edited, each receiving its own version bump with a stacked, self-contained changelog (Card 01: 2.0→3.0, Card 02: 2.0→3.0, Card 03: 3.0→4.0, Card 04: 2.0→3.0, Card 05: 1.0→2.0 — its first edit since freeze). One real catch during drafting: Card 02 has no §16 (that section lives in Card 06, Registry Integrity) — corrected to anchor the registry-file blast-radius statement in Card 02 §5 instead, where the Registry and binding rule actually live, before any edit was applied — a reminder to verify section numbers against the live file rather than the matrix's shorthand citations when drafting new content. Matrix's Blast-Radius row updated to reflect the now-distributed, fully-documented state across all four components. Stage 6 exit criteria met — every major component the rubric names now has an explicit, stated degradation behavior. |
| 2026-06-26 | Ran Closure Plan Stage 5 (Operationalize the Untrusted-Context-Boundary Mechanism) | `docs/reference/03-context-memory.md`, `docs/reference/06-security.md`, `docs/governance/architecture-traceability-matrix.md`, this plan | Closed the last live Stage 1 defect. Added Card 03 §12 ("Source Boundary Preservation") defining a `source_type` tagging convention (system/tool_output/retrieved/memory/a2a_message/user_upload) and an explicit non-override rule — no non-`system` source may be interpreted as overriding a `system`-tagged instruction, regardless of phrasing — giving Card 06 §26's untrusted-context-boundary requirement an actual mechanism rather than an assertion. Enforcement authority for violations is correctly deferred to Card 06, consistent with §8/§11's existing deferral pattern. Card 06 §26's citation corrected from the vague "Card 03 §2-3" to the precise "Card 03 §12." Both cards received a second edit each this cycle — Card 03 now Version 3.0 (1.0→2.0 Stage 4 LangGraph relabel, 2.0→3.0 Stage 5 new §12), Card 06 now Version 3.0 (1.0→2.0 Stage 4 Alt-Architecture note, 2.0→3.0 Stage 5 citation fix) — both with stacked, self-contained changelog histories per the convention established this session. Matrix's Untrusted-Context-Boundary row replaced with a new resolved row tracking the §12 mechanism directly. Stage 5 exit criteria met — zero remaining Stage 1 defects of any kind. |
| 2026-06-26 | Ran Closure Plan Stage 4 (Document Reconciled Anti-Bloat Decisions) | `docs/reference/02-tools-mcp.md`, `docs/reference/04-skills-framework.md`, `docs/reference/06-security.md`, `docs/reference/03-context-memory.md`, `docs/governance/architecture-traceability-matrix.md`, this plan | Checked against the matrix before editing — three of these four rows were "Pending — Stage 4" with no live defect, since these are documented decisions, not contradictions. Added "Alternative Architecture Considered" notes to both sides of the Tool/Skill Registry split (Card 02 §5, Card 04 §6) and to the Circuit Breaker/Policy Server split (Card 06 §11), each citing the reconciled reasoning from `review-reconciliation.md` (Decide/Act separation for the latter, executable-capability vs. procedural-knowledge governance semantics for the former). Relabeled Card 03 §4's LangGraph-specific passage as `Implementation Note (LangGraph)`, leaving the general ADK-comparison sentence as unscoped prose — preserves content, makes framework-coupling visible per the Synthesis's D3 resolution. Four cards bumped to Version 2.0 each (Card 02, 04, 06, 03) with individual changelog notes — no card bumped without an actual content change. Matrix's two Stage-4 rows updated to reflect completion. Stage 4 exit criteria met — these are now intentional, citable decisions rather than undocumented choices that could be mistaken for oversights in a future review. |
| 2026-06-26 | Ran Closure Plan Stage 3 (Resolve Factual Contradictions) | `docs/reference/01-architecture-taxonomy.md`, `docs/governance/architecture-traceability-matrix.md`, this plan | Checked the one live Stage 1 defect against the Traceability Matrix before fixing, per the matrix's governing rule. Card 01 §8's stale observability field list (still including "evaluation score," contradicting Card 05 §5's schema) replaced with a pure pointer to Card 05 §5 — implementing the Synthesis's A1 principle of one canonical definition, not duplicated restatement. Card 01 bumped to Version 2.0 with changelog note. Matrix's Observability Standard row updated to reflect the fix. Card 06 §26's citation of Card 03 §2-3 remains correctly out of scope for this stage — it requires a new Card 03 subsection (Stage 5) before the citation itself can be corrected, not a Stage 3 fix. Re-ran the "search all cards for 'Tier'" and "evaluation score" sanity checks per Stage 3's exit criteria — no other occurrences found. Stage 3 exit criteria met. |
| 2026-06-26 | Ran Closure Plan Stage 2 (Architecture Traceability Matrix) | `docs/governance/architecture-traceability-matrix.md`, this plan | Created the matrix, seeded directly from Stage 1's audit table — populated with every concept identified as having a single canonical owner (Risk Tiers, Agent/Tool/Memory/Skill Contracts, Memory Types, Evaluator Hierarchy, Formal Failure Taxonomy, Data Classification inheritance, HITL unification, etc.), each row carrying its canonical owner and every referencing card/section. Both Stage 1 defects (Card 01 §8's stale "evaluation score" field; Card 06 §26's citation of a not-yet-defined Card 03 mechanism) are carried into the matrix as explicitly flagged STALE rows with their scheduled fix stage noted, rather than silently dropped. Every Stage 4-9 pending deliverable from the Closure Plan's summary table (Alternative-Architecture notes, LangGraph relabeling, blast-radius statements, the four foundational files, Card 06 index, ADRs, verification spec) was pre-seeded as a row marked Pending with its stage, so nothing from the remaining plan can be lost track of. Directory structure section updated to reflect the matrix now exists. This matrix is now the required lookup tool for every fix from Stage 3 onward — no fix may be marked done without checking it here first and updating its "Last Verified" column after. |
| 2026-06-26 | Ran Closure Plan Stage 1 (Full Forward-Reference Audit) | this plan | Audited every "See Card X §Y" / "per Card X" cross-reference across all 7 cards against the three known findings plus an exhaustive sweep for additional staleness. Result: only 2 of the 3 tracked findings are still live defects. Card 01 §8's observability field list still includes "evaluation score," contradicting Card 05 §5's schema, which explicitly excludes it — carried forward to Stage 3. Card 06 §26's citation of Card 03 §2-3 for "source boundary preservation" is confirmed stale — no such mechanism exists in Card 03 §2-3 — carried forward to Stage 5, where the missing abstraction gets drafted before the citation is corrected. The third tracked finding, Card 07 §6's Tier 3/4 citation of Card 06 §3, is verified ALREADY CORRECT in the current file — it reads "Tier 4+ by default (Card 06 §3), or Tier 3 specifically where Card 06 §14's regulated-project override applies," matching Card 06 §3/§14 exactly. This corrects the prior entry's implication that this fix was still pending; no card edit was needed, only this log correction. No other cross-reference in the ecosystem was found stale during the exhaustive pass. Stage 1 exit criteria met — this audit table is the direct input to Stage 2.
| 2026-06-26 | Ran v1 cohesion review cycle (2 independent reviews + reconciliation + 10-stage closure plan); reorganized `docs/governance/` | `docs/governance/cohesion-reviews/v1/*`, `docs/governance/PHASE-0-CLOSURE-PLAN.md`, this plan | Conducted my own independent 4-part cohesion review against the frozen rubric, scoring all 14 dimensions with cited evidence — overall verdict Not-Yet-Freeze-Ready, driven by two confirmed factual contradictions (Card 07 §6 misquoting Card 06 §3's risk tiers; Card 01 §8's stale evaluation-score field contradicting Card 05 §5's fix) and three referenced-but-undrafted foundational files (AGENTS.md, agent-contract-template.md, model-routing-table.md). A separately-run ChatGPT 4-part review scored uniformly high (9.3-10/10 every dimension) and verdicted Freeze-Ready/Approved — direct comparison confirmed this was not a disagreement but a miss: ChatGPT's review did not catch either confirmed contradiction. A Review Board Synthesis document reconciled both reviews into Must-Do/Should-Do/Future/Reject categories; I responded formally in `review-reconciliation.md`, conceding two of my own findings (withdrew the Circuit-Breaker/Policy-Server merge recommendation entirely — Card 01 §2's existing Decide/Act split makes the separation correct; downgraded the Tool/Skill Registry merge from "should fix" to "should document why kept separate") and adopting two new artifacts neither original review proposed (Architecture Decision Records, a Traceability Matrix). Produced `PHASE-0-CLOSURE-PLAN.md` — a 10-stage sequential remediation plan (not independent patches) where each stage's output is a dependency for the next: audit → traceability matrix → fix contradictions → document reconciled decisions → operationalize the one genuine missing mechanism → close blast-radius gaps → draft the 4 missing foundational files (deliberately late, against a now-corrected base) → Card 06 index + ADRs → verification spec → final re-scored confirmation review. Reorganized `docs/governance/` into a versioned structure (`cohesion-reviews/v1/`) so future review cycles don't mix with this one. File 0.14 updated to `IN REVIEW` (Closure Plan Stages 1-9 remain). |
| 2026-06-25 | Approved Card 07; drafted, reviewed, and froze the Ecosystem Cohesion Review Rubric at v1.0 | `docs/reference/07-spec-driven-production.md`, `docs/governance/ecosystem-cohesion-review-rubric.md`, this plan | Card 07 (Spec-Driven Production) APPROVED after one senior-architecture-review pass (10/10 architecture, 10/10 cross-card cohesion) — three polish refinements applied: "code is disposable" reworded to "implementation is regenerable," a Specification Lifecycle added (§12, completing the lifecycle pattern every other artifact type already has), and fleet-governance rollout explicitly bound to §9's canary discipline. One real cross-card inconsistency caught and fixed during my own follow-up cohesion check: Card 07 §6 incorrectly claimed Tier 3+ requires human review by default, when Card 06's own table starts HITL at Tier 4 — corrected. Separately, commissioned and froze a 14-dimension Ecosystem Cohesion Review Rubric across three review rounds (10 dimensions → +4 production dimensions [Architectural Simplicity, Evolutionary Resilience, Blast Radius Analysis, Operational Validation Readiness] → 9 governance refinements [Confidence, Assumptions, Evidence Quality, Architectural Debt Register, Missing-vs-Incorrect split, Alternative-Architecture-Considered, framework-replacement scenario, partial-degradation testing, measurable pass criteria] → Reviewer Charter + mandatory freeze-gate rule + reviewer-independence/version declarations). This rubric was not part of the original Phase 0 plan — it emerged organically from the review discipline itself and is explicitly designed to be reusable beyond this project. New Phase 0 files added: 0.13 (the rubric itself, frozen) and 0.14 (the cohesion review cycle, not yet run). Phase 0's exit criteria updated to distinguish individual-card approval from ecosystem-level cohesion as two separate gates — sequencing corrected so Card 07 approval precedes rubric creation in the documented history, per explicit instruction. |
| 2026-06-25 | Drafted, reviewed, and revised Card 06 across three review cycles | `docs/reference/06-security.md`, this plan | Card 06 (Security & Governance) APPROVED after three senior-architecture-review passes — the most heavily scrutinized card in the ecosystem, appropriately given Projects 1 (healthcare) and 3 (financial) exposure. Round 1 ("brutal review") demanded 10 required + 7 recommended fixes: Policy Server fail-closed behavior by tier, Circuit Breaker state-ownership boundary (signals Harness, never mutates state directly), JIT token full lifecycle, registry runtime verification, instruction-artifact signing (AGENTS.md/contracts/Skills/prompts), A2A delegation semantics, tightened Tier 2/3 defaults for regulated projects, security event schema, clarified egress governance, and explicit Green Team definition. Round 2 pushed for true 10/10 — "who secures the security layer itself" — adding Policy Server integrity/change control (signed policies, Tier 5 dual control), classification inheritance (monotonic, derived artifacts inherit highest source classification), HITL reviewer separation-of-duties, hardened break-glass (time-boxed/auto-expiring/notified), full audit decision provenance, classification-aware model routing, the untrusted-context-boundary principle, expanded supply-chain scope, and Red Team finding governance. Round 3 was five precision patches: removed ambiguous §7a/§8a pseudo-references, added a regulated-project override footnote to the global risk-tier table, strengthened Tier 5 secondary verification from optional to required, protected the Tier 0 local fallback policy from becoming a bypass path, and distinguished automated initial classification from human-reviewed downgrade. This card discharges every outstanding forward-reference from Cards 01-05 in full. |
| 2026-06-25 | Drafted, reviewed, and revised Card 05 | `docs/reference/05-quality-evaluation.md`, this plan | Card 05 (Quality & Evaluation) APPROVED after one senior-architecture-review pass focused on cross-card consistency (per the review-strategy shift logged in the Card 04 entry below). Discharged two cross-card debts: closed the formal Failure Taxonomy gap flagged by both Card 01 and Card 03 (with subcode support and human-approval-failure subtypes added per review), and discharged Card 04's Skill Certification dependency by reusing the Evaluator Hierarchy. Fixed one genuine architectural leak caught by review: removed `evaluation score` from the required observability schema — observability and evaluation are now cleanly separated with a `run_id`/`trace_id` join key, satisfying Card 01 §8's binding requirement without ambiguity. Tightened HITL-unification wording to "unified at the infrastructure/mechanism level, not as one identical workflow" across all 5 trigger points (Cards 01, 02, 03, 04, 05). Added Card 06 forward-references for Safety pillar enforcement boundary and Agent-as-Judge trace-access redaction. Quality Flywheel's Improve step now targets named contract artifacts, consistent with the ecosystem's contract-driven pattern. |
| 2026-06-25 | Drafted, reviewed, and revised Card 04 | `docs/reference/04-skills-framework.md`, this plan | Card 04 (Skills Framework) APPROVED after one senior-architecture-review pass — strongest review yet (9.8/9.2). Answered all procedural-memory-to-Skill promotion questions from Card 03 explicitly (human-reviewed suggestion only, never autonomous). Added Skill Registry mirroring Card 02's Tool Registry, and a Skills-vs-prompt-templates boundary clarification. Confirmed the Agent→Tool→Memory→Skill contract pattern is now a consistent architectural signature across Cards 01-04. Deferred: skill dependencies, freshness tracking, deeper composition precedence (Phase 3); formal skill validation/certification flagged as a Card 05 dependency. Review strategy shifts for Cards 05-07: emphasis moves to cross-card consistency and hidden contradictions rather than missing content per-card. |
| 2026-06-25 | Drafted, reviewed, and revised Card 03; fixed Card 01 terminology | `docs/reference/03-context-memory.md`, `docs/reference/01-architecture-taxonomy.md`, this plan | Card 03 APPROVED after two senior-architecture-review passes. Added memory metadata (confidence/source/timestamp/verification_status), Retrieve→Rank→Filter→Compress→Inject retrieval pipeline, Consolidate→Validate→Store consolidation process, context budgeting, source precedence with regulated-domain exception, memory contracts, shared-history write-hygiene labels, and LLM-consolidation provenance requirement. Renamed "structured state" to "Structured Records" to avoid colliding with Card 01's `state` — Card 01 corrected to match. Tracked forward: context budget defaults and verification-status transitions (Phase 1), procedural-memory-to-Skill promotion rules (now a stated requirement for Card 04). |
| 2026-06-25 | Drafted, reviewed, and revised Cards 01 and 02 | `docs/reference/01-architecture-taxonomy.md`, `docs/reference/02-tools-mcp.md`, this plan | Both cards APPROVED after multiple senior-architecture-review passes. Card 01 elevated Agent Contract to a core component alongside Model/Tools/Orchestration and defined State; established the Runtime Stack governing structure for Cards 02-06. Card 02 added Tool Registry (with binding rule + assignment-authority clarification), Tool Lifecycle, versioning, MCP version pinning, and data minimization, while correctly deferring risk tiers/permissions/sandboxing/A2A trust to Card 06. New artifact added to Phase 0: `docs/architecture/agent-contract-template.md` (0.12). Deferred items flagged in Phase 4 (lifecycle transition criteria, tool certification tests). |
